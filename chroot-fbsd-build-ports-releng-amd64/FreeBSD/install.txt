
# cd to config dir (mounted usb flash, nfs, smbfs, ...)
./mount_and_mkdir.sh

#   releng/11.0
#   amd64
./extract_dists.sh /mnt /path/to/dists
./config.sh /mnt

./unmount.sh

mount -F /chroots/chroot-fbsd-build-ports-releng-amd64/etc/fstab -a
install -d -m 755 -o root -g wheel /chroots/chroot-fbsd-build-ports-releng-amd64/var/run
cp -f /var/run/resolv.conf /chroots/chroot-fbsd-build-ports-releng-amd64/var/run/resolv.conf

chroot /chroots/chroot-fbsd-build-ports-releng-amd64 env -i HOME="/root" SHELL="/bin/sh" PATH="/bin:/usr/bin" TERM="$TERM" USER="root" /bin/sh -c "cd /root; . ./.profile; exec sh -i"

PS1="[chroot@$(hostname -s)]# "
/etc/rc.d/var start
/etc/rc.d/ldconfig start

# cd to config dir (mounted usb flash, nfs, smbfs, ...)
./write_ports_options.sh
./build_ports.sh

exit

umount -F /chroots/chroot-fbsd-build-ports-releng-amd64/etc/fstab -a
