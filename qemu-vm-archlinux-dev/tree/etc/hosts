
# Host Database
::1			REPLACE_HOSTNAME[qemu-vm-archlinux].REPLACE_DOMAIN REPLACE_HOSTNAME[qemu-vm-archlinux] localhost
127.0.0.1		REPLACE_HOSTNAME[qemu-vm-archlinux].REPLACE_DOMAIN REPLACE_HOSTNAME[qemu-vm-archlinux] localhost
