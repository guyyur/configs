
# cd to config dir (mounted usb flash, nfs, smbfs, ...)
./partition.sh dev
./newfs.sh dev
./mount_and_mkdir.sh dev

#   head armv6
./extract_dists.sh /mnt /path/to/dists
./config.sh /mnt

./unmount.sh

mount /usr/ports
mount -t tmpfs -o size=... tmpfs /usr/wrkdir

mount -u -o rw,noatime /

# cd to config dir (mounted usb flash, nfs, smbfs, ...)
./write_ports_options.sh
./build_ports.sh

mount -u -o ro,noatime /

umount /usr/wrkdir
umount /usr/ports
